name: awoke_learning_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  carousel_slider: ^4.2.1
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  flutter_native_splash: ^2.4.0
  flutter_otp_text_field: ^1.1.3+2
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  lottie: ^3.1.0
  provider: ^6.1.2
  salomon_bottom_bar: ^3.3.2

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets\json\animation1.json
    - assets\json\animation2.json
    - assets\json\animation3.json
    - assets\json\accounts.json
    - assets\json\kg1.json
    - assets\json\kids1.json
    - assets\images\awoke hd.svg
    - assets\images\google.svg
    - assets\images\happy.png
    - assets\images\mentor.svg
    - assets\images\mentorplaceholder.png
    - assets\images\kg.svg
    - assets\images\kids.svg
    - assets\images\schoolteens.svg
    - assets\images\grownups.svg
    - assets\images\veterans.svg
    - assets\images\user.svg
    - assets\images\classroom.svg
    - assets\images\school-material.svg
    - assets\images\drawer.svg
    - assets\images\love.svg
    - assets\images\centrallogin.svg
    - assets\images\menu.svg
    - assets\images\whatsapp.svg
    - assets\json\grownups1.json
    - assets\json\schoolteens1.json
    - assets\json\veterans1.json
   
    
   
  #   - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
